#include <stdio.h>

int main(void)
{
	// printf("To C, or  not to C: that is the question\n");
	printf("To C, or not to C: ");
	printf("that is the question.\n");
	return 0;
}

//void means no arguements.
//int is to define the type of value the func main will return.
//return 0; causes the func to terminate 
//and indicates the return value is 0
//without the return statement the program will still terminate
//but compilers will produce a warning msgh because int expected
//";" shows where the statement ends except for compound statements
/* this is a comment */
//declarations must precede statements; not necesarry for C99